package com.mehmetbalbay.bitcointicker.utils

object Const {
    const val BT_DB_NAME = "BitcoinTicker.db"

    // Shared Pref Key
    const val DEFAULT_LANGUAGE = "defaultLanguage"
    const val DEFAULT_CURRENCY = "defaultCurrency"
}